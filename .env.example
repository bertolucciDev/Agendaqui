VITE_API_URL=/agendaqui-api/

# Modo demo: sem backend, dados mockados em memória + localStorage.
# true  -> npm run dev (mock) / npm run build:demo
# false -> API real (npm run dev:real / npm run build)
VITE_USE_MOCK=false
